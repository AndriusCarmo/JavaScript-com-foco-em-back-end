const alunos =['João', 'Juliana', 'Caio', 'Ana'];
const medias =[10, 8, 7.5, 9];

const lista =[alunos,medias];

console.log(
    `A aluna na posição de index 1 é ${lista[0][1]}.
     a média desta aluna é ${lista[1][1]}`
    );

